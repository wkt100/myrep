# 大语言模型 (LLM) 全维度学习笔记

## 1. 诞生过程：从“懂王”到“助手”

大模型的培养分为三个核心阶段：

- **预训练 (Pre-training)**：在海量互联网文本上进行“文字接龙”，学习概率分布，获得基础的世界知识。
- **指令微调 (SFT)**：通过人工编写的问答对，教会模型遵循指令并以对话的形式回答问题。
- **人类反馈强化学习 (RLHF)**：通过人类打分，让模型学习道德、偏好和安全性，实现价值观对齐。

## 2. 底层数学心脏：概率与注意力

- **概率计算 (Softmax)**：神经网络为每个可能的下一个词打分（Logits），通过 Softmax 函数转化为总和为 100% 的概率分布，并以此“掷骰子”选词。
- **注意力机制 (Attention)**：模型通过 **Query (查询)**、**Key (键值)** 和 **Value (数值)** 的匹配，在阅读时为不同位置的词分配权重（聚光灯），从而理解上下文的深层逻辑。

## 3. 多模态：视觉与生成的魔法

- **视觉理解 (ViT)**：将图片切成小方块（Patches），利用**位置编码 (Positional Encoding)** 标记坐标，转换成模型能理解的序列。
- **图像生成 (Diffusion)**：通过在图像中添加噪声并学习如何“去噪”，在文字提示词（Prompt）的引导下，从混沌中提取出符合要求的画面。

## 4. 提示词工程 (Prompt Engineering)

- **角色设定**：锚定模型注意力，改变概率倾向。
- **少样本提示 (Few-shot)**：通过提供示例直接控制输出格式。
- **思维链 (CoT)**：通过“一步步思考”让模型将中间推理过程转化为上下文，提高复杂逻辑的准确率。

## 5. 幻觉与 RAG (检索增强生成)

- **幻觉产生原因**：模型基于概率预测，在知识盲区会通过语言惯性进行“盲目自信”的编造。
- **RAG 方案**：将任务变为“开卷考试”。
  - **向量嵌入 (Embeddings)**：将文字转换为多维空间坐标。
  - **混合检索**：结合语义相似度和元数据标签（如时间、作者）精准提取知识库信息。

## 6. AI 智能体 (Agents) 与协作

- **ReAct 模式**：思考 (Thought) -> 行动 (Action) -> 观察 (Observation) 的循环。
- **状态机管理 (FSM)**：用严格的流转逻辑（节点和边）管理 Agent 行为，防止无限循环和数据竞争。
- **工具调用 (Function Calling)**：大模型通过输出 JSON 指令来请求外部系统执行 API 任务。
- **多智能体协作**：通过分工（如程序员、测试员、仲裁者）提高效率，减少注意力稀释。

## 7. 未来进化方向

- **System 2 思考**：引入类似 AlphaGo 的推理过程，让模型在回答前进行自我博弈和深度推演。
- **具身智能 (Embodied AI)**：大模型与物理实体结合，通过多模态理解直接操控硬件。
- **混合专家模型 (MoE)**：通过路由器动态激活特定专家子网络，实现“又大又快”的推理。